{
  "name": "airllm-skill",
  "version": "1.0.0",
  "description": "Generación de texto usando modelo local GPU (AirLLM)",
  "main": "index.js",
  "tools": [
    {
      "name": "generate",
      "description": "Genera texto usando modelo de lenguaje local en GPU",
      "input_schema": {
        "type": "object",
        "properties": {
          "prompt": {
            "type": "string",
            "description": "Prompt para generación de texto"
          }
        },
        "required": ["prompt"]
      }
    }
  ]
}
